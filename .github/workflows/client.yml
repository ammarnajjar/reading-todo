name: Client-CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - name: Run tests
        run: |
          cd client
          npm ci
          npm run test --ci
      - name: Build Client
        run: |
          cd client
          npm ci
          npm build
